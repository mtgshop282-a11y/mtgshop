services:
  - type: web
    name: mtgshop
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn run:app"
    runtime: python
    pythonVersion: 3.11

